/**
 * decodes address of a chain
 *  hexAddress length cannot be more than 57 bytes
 * @param chain
 * @param hexAddress
 */
export const addressDecoder = (chain: string, hexAddress: string) => {
  throw Error(`not implemented`);
};
